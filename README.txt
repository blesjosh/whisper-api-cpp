Before building this project:
1. Download `base.en.bin` from:
   https://huggingface.co/ggerganov/whisper.cpp/resolve/main/models/base.en.bin
2. Create a folder named `models` in the root.
3. Put `base.en.bin` inside the `models` folder.
4. Then build the Docker image.
